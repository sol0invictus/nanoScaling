# GDN token-mixer study: layer 0 attention + layer 1 Gated DeltaNet (the requested config)
task: dihedral_48
train_frac: 0.3
weight_decay: 1.0
seed: 0
optimizer: adamw
n_layer: 2
n_embd: 128
n_head: 4
lr: 0.001
betas:
- 0.9
- 0.98
warmup_steps: 10
batch_size: 512
max_steps: 30000
cheap_every: 25
expensive_every: 250
n_checkpoints: 30
device: cuda
layer_types: attn,gdn
out_dir: runs/g_attn_gdn
run_name: g_attn_gdn
tags:
  purpose: layer 0 attention + layer 1 Gated DeltaNet (the requested config)
